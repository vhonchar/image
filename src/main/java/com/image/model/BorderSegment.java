package com.image.model;

import java.awt.image.BufferedImage;

public class BorderSegment extends Segment {

    public BorderSegment(BufferedImage image, int xStart, int yStart, int size) {
//        int[] rgbArray = image.getRGB(xStart, yStart, size, size, null, 0, 0);
//
//        this.segment = new int[size][size];
//        for(int i = 0; i < rgbArray.length; i++){
//            this.segment[i/size][(i + size) % size] = rgbArray[i];
//        }
    }
}
